# Example environment variables
# Copy this to .env.local and fill in actual values if needed

# API Configuration (if adding external APIs later)
# NEXT_PUBLIC_API_URL=http://localhost:3000

# Frame Asset Configuration
NEXT_PUBLIC_FRAMES_PATH=/frames/
